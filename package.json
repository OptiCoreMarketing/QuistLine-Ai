{
  "name": "quistline-ai",
  "version": "1.0.0",
  "description": "QuistLine.ai Lead Agent Workspace med Chief, Engineer og Groq Engine",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --env-file-if-exists=.env tests/server.test.js && node --env-file-if-exists=.env tests/stateMachine.test.js && node --env-file-if-exists=.env tests/guards.test.js && node --env-file-if-exists=.env tests/agentMemory.test.js",
    "migrate": "node --env-file-if-exists=.env scripts/migrate.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "groq-sdk": "^0.5.0",
    "pg": "^8.13.0"
  }
}
